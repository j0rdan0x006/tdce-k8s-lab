apiVersion: v1
kind: Service
metadata:
  name: {{ include "tdce-monitoring.fullname" . }}
  labels:
    {{- include "tdce-monitoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tdce-monitoring.selectorLabels" . | nindent 4 }}
