apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tdce-monitoring.fullname" . }}-test-connection"
  labels:
    {{- include "tdce-monitoring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tdce-monitoring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
